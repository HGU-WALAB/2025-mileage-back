<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.scholarship.mapper.MileageMapper">

    <select id="hasExistingApplication" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM _sw_student
            WHERE student_id = #{studentId}
              AND is_apply = 1
        )
    </select>

    <update id="createApplication">
        UPDATE _sw_student
        SET is_apply = 1,
            apply_date = #{applyDate},
            is_checked = #{isChecked}
        WHERE student_id = #{studentId}
          AND is_apply = 0
    </update>
</mapper>